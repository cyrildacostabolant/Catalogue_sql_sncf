name: Supabase Keep Alive

on:
  schedule:
    # S'exécute tous les jours à 05:00 UTC
    - cron: '0 5 * * *'
  workflow_dispatch: # Te permet de tester manuellement dans l'onglet Actions

jobs:
  ping_supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Recipes Table
        run: |
          curl -i -X GET "https://hovvlztgbkrgmlxwepeo.supabase.co/rest/v1/recipes?select=id&limit=1" \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
